---
apiVersion: v1
kind: Namespace
metadata:
  name: database
  labels:
    # TODO: [flux] Applying labels via flux doesn't work
    # goldilocks.fairwinds.com/enabled: true
    kustomize.toolkit.fluxcd.io/prune: disabled
