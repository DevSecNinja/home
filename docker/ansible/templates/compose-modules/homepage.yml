services:
  homepage:
    container_name: homepage
    image: ghcr.io/gethomepage/homepage:v0.8.7@sha256:06203a4aeb0dac75a8419f86d950ea01f3b9e5ca3f1fcd6c7065e34d3700b05f
    depends_on:
      - traefik
    restart: always
    networks:
      - t2_proxy
    environment:
      # TODOD: [Docker API] Enable Docker API so that Homepage doesn't need to run as root
      # https://gethomepage.dev/latest/configs/docker/
      # - PUID=${PUID}
      # - PGID=${PGID}
      - TZ=${TZ}
    security_opt:
      - no-new-privileges:true
    volumes:
      - $DOCKERDIR/data/homepage:/app/config
      - /var/run/docker.sock:/var/run/docker.sock:ro
    labels:
      # Traefik
      - "traefik.enable=true"
      ## Middlewares
      - "traefik.http.routers.homepage-rtr.middlewares=chain-no-auth@file"
      ## HTTP Services
      - "traefik.http.routers.homepage-rtr.service=homepage"
      - "traefik.http.services.homepage.loadbalancer.server.port=3000"
