services:
  excalidraw:
    image: excalidraw/excalidraw:latest@sha256:b2b642fdaca652cf23c20e07d3603bed5b22b2e1c88a15bec5f92e8a38d9041c
    container_name: excalidraw
    depends_on:
      - traefik
    restart: always
    mem_limit: 1024m
    security_opt:
      - no-new-privileges=true
    environment:
      - PUID=${PUID}
      - PGID=${PGID}
      - TZ=${TZ}
    networks:
      - excalidraw-frontend
    labels:
      # Homepage
      - homepage.group=Productivity
      - homepage.name=Excalidraw
      - homepage.icon=sh-excalidraw
      - homepage.href=https://excalidraw.$DOMAINNAME
      - homepage.description=Virtual Whiteboard
      # Traefik
      - "traefik.enable=true"
      - "traefik.docker.network=excalidraw-frontend"
      ## Middlewares
      - "traefik.http.routers.excalidraw-rtr.middlewares=chain-auth@file"
      ## HTTP Services
      - "traefik.http.routers.excalidraw-rtr.service=excalidraw"
      - "traefik.http.services.excalidraw.loadbalancer.server.port=80"
      ## Router for Gatus — bypass auth if secret header is present
      - "traefik.http.routers.excalidraw-monitor-rtr.rule=Host(`excalidraw.$DOMAINNAME`) && Header(`X-Monitor-Key`, `$GENERIC_MONITORING_HEADER_SECRET`)"
      - "traefik.http.routers.excalidraw-monitor-rtr.service=excalidraw"
      - "traefik.http.routers.excalidraw-monitor-rtr.priority=100"  # Higher priority so it matches first

networks:
  excalidraw-frontend:
    name: excalidraw-frontend
