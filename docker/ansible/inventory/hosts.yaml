---
development:
  hosts:
    svldev.MANAGEMENT:
      ansible_user: "jean-paul"

hypervisor:
  hosts:
    SVLHOST01.MANAGEMENT:
      ansible_user: "root"

docker:
  children:
    webservers:
      children:
        management:
          hosts:
            svlmgmt.MANAGEMENT:
              ansible_user: "jean-paul"
        production:
          hosts:
            svlprod.PRODUCTION:
              ansible_user: "jean-paul"

    dnsservers:
      hosts:
        svlmgmt.MANAGEMENT:
          ansible_user: "jean-paul"
