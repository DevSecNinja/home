---
development:
  hosts:
    svldev.MANAGEMENT:
      ansible_user: "jean-paul"

hypervisor:
  hosts:
    HOST01.MANAGEMENT:
      ansible_user: "root"

docker:
  children:
    webservers:
      children:
        management:
          hosts:
            svlmgmt.MANAGEMENT:
              ansible_user: "jean-paul"
