---
development:
  children:
    management:
      hosts:
        localhost:
          ansible_user: "jean-paul"

docker:
  children:
    webservers:
      children:
        management:
          hosts:
            svlmgmt.MANAGEMENT:
              ansible_user: "jean-paul"
