---
development:
  hosts:
    svldev.MANAGEMENT:
      ansible_user: "jean-paul"

hypervisor:
  hosts:
    SVLHOST01.MANAGEMENT:
      ansible_user: "root"

docker:
  children:
    webservers:
      children:
        linux_amd64:
          children:
            management:
              hosts:
                svlmgmt.MANAGEMENT:
                  ansible_user: "jean-paul"
            production:
              hosts:
                svlprod.PRODUCTION:
                  ansible_user: "jean-paul"
            external:
              hosts:
                svlext.ravensberg.org:
                  ansible_user: "jean-paul"

        linux_arm64_v8:
          children:
            fallback:
              hosts:
                svlfall.MANAGEMENT:
                  ansible_user: "jean-paul"

    dnsservers:
      hosts:
        svlmgmt.MANAGEMENT:
          ansible_user: "jean-paul"
        svlfall.MANAGEMENT:
          ansible_user: "jean-paul"
