---

#
# Generic
#

tz: "Europe/Amsterdam"
generic__hostname_prefix: "{{ inventory_hostname[:3] }}"
generic__hostname_suffix: "{{ inventory_hostname[3:] }}"
generic__hostname_os_type: "{{ inventory_hostname[2] }}"

#
# Sops
#

sops_version: "3.8.1"
